name: simple-workflow
on:
    push:
        branches:
            - main
            - 'feature/**'
            - 'dev-*'
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v6
            - name: show-Info
              run: echo "hello World!!"
